<h1>Lista de Supermercado</h1>

<% if flash[:notice] %>
  <p><%= flash[:notice] %></p>
<% end %>

<ul>
  <% @items.each do |item| %>
    <li>
      <%= item.name %>
      <%= item.quantity %> unidades
      <%= link_to 'Eliminar', item, method: :delete, data: { confirm: '¿Estás seguro?' } %>
    </li>
  <% end %>
</ul>

<%= render 'form' %>

<h1>Carrito</h1>

<ul>
  <% @cart_items.each do |item| %>
    <li>
      <%= item.name %>
      <%= item.quantity %> unidades
    </li>
  <% end %>
</ul>
